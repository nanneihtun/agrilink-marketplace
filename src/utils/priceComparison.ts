import { Product } from '../data/products';

// Price comparison data generator - returns empty array for real data only
export const generatePriceComparisonData = (productName: string, basePrice: number = 45000) => {
  // Return empty array - only real listings should be shown
  return [];
};